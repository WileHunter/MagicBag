<UserControl x:Class="WpfApp3.DatabaseView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:models="clr-namespace:WpfApp3.Models" xmlns:controls="clr-namespace:HandyControl.Controls;assembly=HandyControl" mc:Ignorable="d" d:DesignHeight="480" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="DrawerColumn" Width="0"/>
            <ColumnDefinition Width="24"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <!-- 左侧抽屉 -->
        <Border x:Name="DrawerPanel" Background="#F5F5F5" BorderBrush="#EBEBFA" BorderThickness="0,0,1,0" Grid.Column="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <!-- 顶部按钮 -->
                <Border Grid.Row="0" Background="#EBEBFA" Padding="8">
                    <DockPanel LastChildFill="False">
                        <!-- 左侧标题 -->
                        <TextBlock Text=" 全部文件夹" FontSize="13" FontWeight="Bold" VerticalAlignment="Center" DockPanel.Dock="Left" Foreground="#333"/>
                        <!-- 右侧新建按钮 -->
                        <Button Content="+" Width="30" Height="25" HorizontalAlignment="Right" DockPanel.Dock="Right" Margin="0,0,4,0" Click="AddTopLevelCategory_Click" Style="{StaticResource ButtonInfo}"/>
                    </DockPanel>
                </Border>
                <!-- 分类树（不再在 XAML 内写 ContextMenu，避免 connectionId 异常） -->
                <TreeView x:Name="CategoryTreeView" Grid.Row="1" ItemsSource="{Binding Categories}" SelectedItemChanged="CategoryTreeView_SelectedItemChanged" Background="#F0F4F8" BorderThickness="0" Padding="5" FontSize="14" FontFamily="Microsoft YaHei UI">
                    <TreeView.Resources>
                        <!-- 节点模板：图标 + 名称 -->
                        <HierarchicalDataTemplate DataType="{x:Type models:Category}" ItemsSource="{Binding SubCategories}">
                            <StackPanel Orientation="Horizontal" Margin="4,2">
                                <!--<Image Source="/images/folder.png" Width="16" Height="16" Margin="0,0,5,0"/>-->
                                <TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </HierarchicalDataTemplate>
                        <!-- 仅做轻量的舒适度优化，不改默认模板 -->
                        <Style TargetType="TreeViewItem">
                            <Setter Property="FontSize" Value="10"/>
                            <Setter Property="Padding" Value="5,3"/>
                            <Setter Property="MinHeight" Value="28"/>
                            <Setter Property="Margin" Value="2,2"/>
                        </Style>
                    </TreeView.Resources>
                </TreeView>
            </Grid>
        </Border>
        <!-- 抽屉控制按钮 -->
        <Border Grid.Column="1" Background="#F5F5F5" BorderBrush="#DDD" BorderThickness="0,0,1,0">
            <ToggleButton x:Name="DrawerToggle" Width="24" Height="60" VerticalAlignment="Center" HorizontalAlignment="Center" Content="❯" Background="Transparent" BorderThickness="0" Checked="DrawerToggle_Checked" Unchecked="DrawerToggle_Unchecked"/>
        </Border>
        <!-- 主体区域 -->
        <Grid Grid.Column="2" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <!-- Growl 容器（右上角） -->
            <ScrollViewer VerticalScrollBarVisibility="Hidden" HorizontalAlignment="Right" Grid.ZIndex="100">
                <StackPanel controls:Growl.Token="SuccessMsg" VerticalAlignment="Top" Margin="0,10,10,10"/>
            </ScrollViewer>
            <!-- 工具列表 -->
            <DataGrid x:Name="ToolsGrid" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="True" IsReadOnly="False" Margin="0,0,0,8">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                    <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="160"/>
                    <DataGridTextColumn Header="描述" Binding="{Binding Description}" Width="150"/>
                    <DataGridTextColumn Header="路径" Binding="{Binding ToolPath}" Width="150"/>
                    <DataGridTextColumn Header="运行文件" Binding="{Binding RUN}" Width="100"/>
                    <DataGridTextColumn Header="CMD" Binding="{Binding Cmd}" Width="65"/>
                    <DataGridTextColumn Header="命令" Binding="{Binding Commond}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
            <!-- 操作按钮 -->
            <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="添加工具" Background="#C2C2F0" Margin="5" Click="AddTool_Click"/>
                <Button Content="删除工具" Background="#DB7070" Margin="5" Click="DeleteTool_Click"/>
                <Button Content="保存JSON" Background="#FFFFCC" Margin="5" Click="SaveJson_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>