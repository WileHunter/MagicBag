<UserControl x:Class="WpfApp3.NotebookView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp3"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <Style TargetType="RichTextBox" x:Key="MyRichTextBoxStyle">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="5"/>

            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#007ACC"/>
                </Trigger>
                <Trigger Property="IsKeyboardFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#007ACC"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="MinHeight" Value="28"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <DockPanel Grid.Column="0" Margin="5">
            <ToolBar DockPanel.Dock="Top" HorizontalAlignment="Stretch">
                <Button Content="新增" Click="NewNote_Click" />
                <Button Content="删除" Click="DeleteNote_Click" Margin="5,0,0,0"/>
            </ToolBar>
            <ListBox x:Name="NotesListBox" SelectionChanged="NotesListBox_SelectionChanged">
                <ListBox.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="重命名" Click="Rename_Click"/>
                    </ContextMenu>
                </ListBox.ContextMenu>
            </ListBox>
        </DockPanel>

        <DockPanel Grid.Column="1" Margin="5">
            <ToolBar DockPanel.Dock="Top" HorizontalAlignment="Stretch">
                <Button Content="加粗" Click="Bold_Click"/>
                <Button Content="斜体" Click="Italic_Click" Margin="5,0,0,0"/>
                <Separator/>
                <Button Content="一级标题" Click="Heading1_Click"/>
                <Button Content="二级标题" Click="Heading2_Click"/>
                <Separator/>
                <Button Click="SaveNote_Click">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="/images/save.png" Width="16" Height="16" Margin="0,0,5,0"/>
                        <TextBlock Text="保存"/>
                    </StackPanel>
                </Button>
                <TextBlock x:Name="StatusTextBlock" Margin="10,0,0,0" VerticalAlignment="Center" FontWeight="Bold"/>
            </ToolBar>

            <Border BorderBrush="#DDDDDD" 
                    BorderThickness="1" 
                    CornerRadius="8" 
                    Padding="5" 
                    Background="White">
                <RichTextBox x:Name="NoteRichTextBox"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             DataObject.Pasting="RichTextBox_Pasting"
                             TextChanged="NoteRichTextBox_TextChanged"
                             Background="#eaeded"
                             Style="{StaticResource MyRichTextBoxStyle}" />
            </Border>
        </DockPanel>
    </Grid>
</UserControl>